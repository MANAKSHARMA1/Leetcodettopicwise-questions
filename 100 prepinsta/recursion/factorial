#include <bits/stdc++.h>

using namespace std;
int factorial(int n){
    //int ans =1;
if(n==0)
return 1;
//else
   return n * factorial(n - 1);
}
int main()
{    int n=5;
 cout<< factorial(n);
    //cout<<ans;
    return 0;
}
