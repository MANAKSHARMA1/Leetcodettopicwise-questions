#include<bits/stdc++.h>
using namespace std;
 
int main(){
  int n,d,sum=0;
  cout<<"enter any no.";
  cin>>n;
  while(n!=0){
      d=n%10;
      sum = sum +d;
      n=n/10;
  }
  cout<<"SUM is "<<sum;
  
    return 0;
}

Time Complexity: O(N), N is the no. of digits

Space Complexity: O(1)
