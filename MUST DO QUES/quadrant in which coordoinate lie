
#include <iostream>

using namespace std;

int main()
{
    int n,m;
    cin>>n>>m;
    if(n>0 && m>0)
    cout<<"1st quad"<<endl;
    
    if(n<0 && m>0)
        cout<<"2nd quad"<<endl;

    if(n<0 && m<0)
    cout<<"3rd quad"<<endl;

    if(n>0 && m<0)
    cout<<"4th quad"<<endl;
    
    
    
}
