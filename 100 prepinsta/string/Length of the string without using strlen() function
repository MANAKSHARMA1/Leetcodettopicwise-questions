#include <bits/stdc++.h>

using namespace std;

 
int main()
{
char s[100];
int i;\
cout << "\nEnter a string: ";
cin >> s;
for(i = 0; s[i] != '\0'; ++i);

// '\0' is NULL character

cout << "\nLength of string : " << i;
cout << endl;
return 0;
}
