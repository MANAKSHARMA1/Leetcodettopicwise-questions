#include <bits/stdc++.h>

using namespace std;
int largest(int arr[],int n){
    sort(arr,arr+n);
    if(n==1)
    return arr[0];
   else
     return arr[n-1];
}
int main()
{
    
    int arr[]={3,2,7,4,5};
    int n=5;
   
    cout<< largest(arr,n);

    return 0;
}
